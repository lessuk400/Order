<%= simple_form_for @order do |f| %>
  <%= f.input :user_id, input_html: {value: current_user.id} %>
  <%= f.fields_for :order_meals do |o_m_f| %>
    <%= o_m_f.input :meal_id, collection: Menu.last.meals, label_method: lambda {|meal| "#{meal.food_item.name}"} %>
  <% end %>
  <%= f.submit %>
<% end %>
